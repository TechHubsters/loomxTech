# Example environment variables for the LoomX server
# Copy this to .env and fill with your SMTP details

# SMTP host (e.g., smtp.gmail.com)
SMTP_HOST=smtp.example.com
# SMTP port (465 for SSL, 587 for TLS)
SMTP_PORT=587
# SMTP auth user and password
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# Where to send incoming requests (defaults to SMTP_USER if unset)
TO_EMAIL=hello@loomx.tech

# Optional from email address to show in the outgoing email
FROM_EMAIL=no-reply@loomx.tech
